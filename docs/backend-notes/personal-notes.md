Initially always setup docker first, don't ever connect to locally installed DB, 
use docker from start, even for DB and python installation version.. to keep consisteny, 

create files like Dockerfile, docker-compose.yml, .env, requirements.txt file

